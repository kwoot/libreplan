<?xml version="1.0" encoding="UTF-8"?>
<web-app id="WebApp_ID" version="5.0"
         xmlns="https://jakarta.ee/xml/ns/jakartaee" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd">

    <display-name>libreplan-webapp</display-name>

    <!--
        It searches all libreplan-business-spring-config.xml files, it can found several.
        There must be at least one.

        It searches libreplan-webapp-spring-config.xml.
        There must be just one.

        It searches libreplan-override-spring-config.xml to override some previous definitions.
        There could be several or none.
    -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>
            classpath*:/libreplan-business-spring-config.xml
            classpath:/libreplan-webapp-spring-config.xml
            classpath:/libreplan-webapp-spring-security-config.xml
        </param-value>
    </context-param>

    <!--
        DSP (org.zkoss.web.servlet.dsp.InterpreterServlet) removed: the servlet class itself was
        dropped from ZK's zweb jar starting at 9.6.x (present through 8.6.x, confirmed absent from
        both 9.6.0.2 and 10.3.0.1-jakarta), causing ClassNotFoundException at webapp startup. No
        .dsp files exist anywhere in this app's webapp resources, so this was unused dead config.
    -->

    <servlet>
        <description><![CDATA[Callback Servlet]]></description>
        <servlet-name>callbackServlet</servlet-name>
        <servlet-class>org.zkoss.ganttz.servlets.CallbackServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>callbackServlet</servlet-name>
        <url-pattern>/callback/*</url-pattern>
    </servlet-mapping>

    <!-- Spring security -->
    <filter>
        <filter-name>springSecurityFilterChain</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>springSecurityFilterChain</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <listener>
        <listener-class>org.libreplan.web.LoggingConfiguration</listener-class>
    </listener>

    <!-- ZK Session Cleaner listener: now provided by zkwebfragment's own web-fragment.xml. -->

    <!-- Spring listeners -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <listener>
        <listener-class>org.springframework.web.context.request.RequestContextListener</listener-class>
    </listener>

    <!-- Loads all IDataBootstrap and executes them -->
    <listener>
        <listener-class>org.libreplan.web.bootstrap.BootstrapListener</listener-class>
    </listener>

    <!--
        The ZK page loader (DHtmlLayoutServlet, *.zul/*.zhtml, update-uri=/zkau) and the ZK
        asynchronous update engine (DHtmlUpdateServlet, /zkau/*) used to be declared here by hand.
        ZK 10's zkwebfragment jar now auto-registers identical servlets via its own
        META-INF/web-fragment.xml, and Jetty enforces that a URL pattern maps to exactly one
        servlet - keeping both here throws IllegalStateException: Multiple servlets map to path
        /zkau/* at startup. Removed; zkwebfragment provides them.
    -->

    <!-- CXF -->
    <servlet>
        <display-name>CXF Servlet</display-name>
        <servlet-name>CXFServlet</servlet-name>
        <servlet-class>org.apache.cxf.transport.servlet.CXFServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>CXFServlet</servlet-name>
        <url-pattern>/ws/rest/*</url-pattern>
    </servlet-mapping>

    <session-config>
        <!-- The session timeout is ten minutes. A timer is used to keep the session alive if there is some open page -->
        <session-timeout>10</session-timeout>
    </session-config>

    <welcome-file-list>
        <welcome-file>/common/index.zul</welcome-file>
    </welcome-file-list>

    <error-page>
        <exception-type>java.lang.Throwable</exception-type>
        <location>/common/error.zul</location>
    </error-page>

    <error-page>
        <error-code>404</error-code>
        <location>/common/pageNotFound.zul</location>
    </error-page>

    <error-page>
        <error-code>403</error-code>
        <location>/common/accessForbidden.zul</location>
    </error-page>

</web-app>
